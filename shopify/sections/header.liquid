<header class="site-header">
  <div class="page-width header-inner">
    <a href="{{ routes.root_url }}" class="brand">
      <span class="brand-mark">⚡</span>
      <span class="brand-name">LEKI</span>
    </a>
    <nav class="header-nav" aria-label="Primary">
      <a href="{{ routes.collections_url }}">Shop</a>
      <a href="{{ routes.page_url | append: 'faq' }}">FAQ</a>
      <a href="{{ routes.page_url | append: 'contact' }}">Contact</a>
      <a href="{{ routes.blogs_url }}">Blog</a>
    </nav>
  </div>
</header>
